# Python Dependencies for WhatsApp Agent Project
# This project uses:
# - Python for Claude Agent SDK service on Render

# ============================================
# Core Dependencies
# ============================================

# Claude Agent SDK (official Python SDK for building agents)
claude-agent-sdk>=0.1.0

# Anthropic Claude API (for low-level API access)
anthropic>=0.18.0

# HTTP Requests (for WhatsApp API calls)
requests>=2.31.0

# Environment Variables
python-dotenv>=1.0.0

# ============================================
# Web Service Dependencies (Render Deployment)
# ============================================

# FastAPI for REST API
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# ============================================
# MCP Server Dependencies
# ============================================

# Model Context Protocol SDK
mcp>=1.18.0

# ============================================
# GitHub Integration (Optional for validation)
# ============================================

# For GitHub token validation in github_mcp/server.py
# Note: The actual GitHub MCP runs via HTTP, so no extra deps required
# But we use requests for token validation
# (requests is already included above)

# ============================================
# Optional: Session Management
# ============================================

# Redis (optional - for production session storage)
# redis>=5.0.0

# ============================================
# Development & Testing
# ============================================

# pytest (for testing)
# pytest>=7.4.0
